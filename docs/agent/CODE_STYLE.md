# Code style

- TypeScript strict mode is expected (see `tsconfig.json`).
- Prefer functional patterns when they keep code simpler.
- Formatting and linting are handled by Biome: run `pnpm format` and `pnpm lint`.
